# Root-level secrets — each project dir (openclaw/, chatwoot/, openchat-bridge/)
# has its own .gitignore covering its own .env/node_modules/build output.
.env
.scratch/
# Scratch docker env dumps (docker inspect output) — not source, may contain
# local container secrets/passwords.
deploy/.tmp-env/
